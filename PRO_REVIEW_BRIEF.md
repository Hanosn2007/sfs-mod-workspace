# 请求 Pro 分析：Spaceflight Simulator 多选零件参数编辑

## 用户目标与确定的规则

- 在游戏建造页的 Build Tools 浮窗中，多选零件后直接编辑参数，保留 Part / Build / JSON 三页，以及原有单零件编辑能力。
- 部分适用字段要显示 `Applies n/N`。例如选中 2 个 Fuel Tank + 1 个 Engine Titan 时，`Fuel (%)` 只改两个燃料箱；发动机仍选中、不增加燃料字段。发动机开关只改实际带字段的发动机。
- 同名字段只有 `PartSave.name + 字段类型 + 原始键` 均相同时才合并。混合值不因选中、打开面板或空输入而自动统一。原始字段必须可访问；JSON 页仍只编辑单零件。
- 用户明确指出：单零件参数原本自动生效，多选不应要求每行点 Apply。不同数据类型应采用合适的输入方式，并提高可读性。
- 最新人工反馈：现有多选的**数值和开关控件摆放、点击手感仍不顺**；功能本身可用。

## 当前实现与验证边界

- 代码重点：`BuildTools/src/PartEditor/MultiPartGUI.cs`、`GUI.cs`、`BuildTools/src/TabbedPanel.cs`、`UITools/Elements/NumberInput.cs`。
- 当前多选页把常用 Fuel (%) 和 Engine Titan 的三个开关放在前面；其余保存变量放在可展开的 raw fields。数值框回车/失焦写入，± 每按一次相对调整各适用零件；布尔字段显示 Mixed/On/Off，旁边是 Set Off/Set On；文本回车/失焦写入，Clear 可明确清空。位置通过 Step + X-/X+/Y-/Y+ 相对移动。每次即时改动各自形成一个游戏撤销步骤。
- 现有一行字段被拆成“字段名与适用数”“状态解释”“输入/按钮”多层，占高度，尤其多个开关连续出现时很啰嗦。窗口最低为 400×450，可拖角连续缩放；内容区可滚动，顶部三页按钮固定。不能假定窗口永远很宽。
- 已在 macOS 游戏中验证：2 燃料箱 + 1 发动机字段显示/适用计数正确；Fuel 40 只写两个燃料箱，一次撤销恢复；本轮即时输入、±、开关和原始字段展开路径功能正常。**视觉和点击手感尚未通过用户验收。**
- 此仓库源码包含一处尚未装入游戏的小修复：失焦提交前检查用户是否实际修改文本，防止外部数值变化时旧显示值写回。当前游戏安装 DLL 是此修复之前的版本；请勿把代码编译成功当作它已在游戏验证。
- Windows 手动交接包目前仍是旧的 BT-014 基线，尚不包含本轮多选编辑。Windows 实机加载、Harmony 补丁和输入均未验证。macOS 上 Snap to Parts、Part Adaptation、Grid Snap 因旧 Harmony 补丁跳过而无效，这是另一问题，不要把其修复作为本次 UX 建议的前提。

## 请回答

1. 对数值、混合数值、布尔/混合布尔、文本/混合文本、部分适用字段和相对移动，分别给出适合该浮窗的输入控件、提交时机、可见状态、撤销粒度。尤其避免敲数字过程中的中间值误写，并避免开关需要远距离重复点按钮。
2. 给出 400×450 小窗口和放大窗口的具体信息层级/布局草图（文字或 ASCII 即可）。说明字段名、`Applies n/N`、当前值/混合状态和操作控件在一行或两行中的位置，以及长原始键如何处理。
3. 从现有 `MultiPartGUI.cs` 和 SFS ModGUI/UITools 能力看，最小可运行的改动顺序是什么？哪些体验建议需要新增控件或改动 UITools，哪些可用现有控件完成？
4. 指出本轮最容易误伤的交互：混合值、失焦、拖窗/滚动、撤销、数值边界、选区变化、原始字段写入。请给出 2–3 个最有信息量的游戏内手动验收场景。

请先说明你是否实际能读取这个私有 GitHub 仓库的源码。如果不能，明确说未读取仓库，仅根据这份摘要与随后提供的关键代码分析。建议要可在现有项目里落地，区分已验证事实与推断；不需要设计自动安装器。
